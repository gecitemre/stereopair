# Only needed for ./deploy-peer.sh, which installs the receiver on the other Mac.
PEER="you@other-mac.local"

# Optional overrides:
# TARGET_MS=20      # force a buffer instead of letting the link decide
# IO_FRAMES=128
